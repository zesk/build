#!/usr/bin/env bash
# Copyright &copy; 2026 Market Acumen, Inc.
# Generated on 2026-06-20
# shellcheck disable=SC2034
argument=$'expected - String. Required. A string NOT to expect in the output\nbinary - Callable. Required. Binary to run and evaluate output\n... - Arguments. Optional. Any additional arguments to `binary`.\n--exit - Assert exit status of process to be this number\n--stderr - Also include standard error in output checking\n--help - Flag. Optional. Display this help.\n--handler handler - Function. Optional. Use this error handler instead of the default error handler.\n--display - String. Optional. Display name for the condition.\n--debug - Flag. Optional. Debugging enabled for the assertion function.\n--line lineNumber - Integer. Optional. Line number of calling function. Typically this is not required as it is computed from the calling function using `--line-depth`.\n--line-depth depth - Integer. Optional. The depth in the stack of function calls to find the line number of the calling function.\n--stdout-match - String. Optional. One or more strings which must match `stdout` output.\n--stdout-no-match - String. Optional. One or more strings which must match `stdout` output.\n--stderr-ok - Flag. Optional. Output to `stderr` will not cause the test to fail.\n--stderr-match - String. Optional. One or more strings which must match `stderr` output. Implies `--stderr-ok`\n--stderr-no-match - String. Optional. One or more strings which must match NOT `stderr` output. Implies `--stderr-ok`\n--dump - Flag. Optional. Output `stderr` and `stdout` after test regardless.\n--dump-binary - Flag. Optional. Output `stderr` and `stdout` after test regardless, displayed as binary.\n--plumber - Flag. Optional. Wrap the test call with the `plumber` call to detect local leaks.\n--leak globalName - Zero or more. String. Allow global leaks for these globals when `--plumber` is enabled.\n--skip-plumber - Flag. Optional. Skip plumber check for function calls. When specified with `--plumber` the last occurrence on the command line is effective.\n--head - Flag. Optional. When outputting `stderr` or `stdout`, output the head of the file.\n--tail - Flag. Optional. When outputting `stderr` or `stdout`, output the tail of the file. (Default)\n'
base="test.sh"
derivations=([0]="return_code" [1]="fn" [2]="lowerFn" [3]="fnMarker" [4]="argument" [5]="usage")
description=$'Run a command and expect the output to not contain the occurrence of a string.\n\nIf this fails it will output the command result to stdout.\n\n'
descriptionLineCount="4"
example=$'    assertOutputDoesNotContain Success complex-thing.sh --dry-run\n'
file="bin/build/tools/test.sh"
fn="assertOutputDoesNotContain"
fnMarker="assertoutputdoesnotcontain"
foundNames=([0]="argument" [1]="return_code" [2]="example" [3]="reviewed")
line="953"
original="assertOutputDoesNotContain"
rawComment=$'Run a command and expect the output to not contain the occurrence of a string.\nIf this fails it will output the command result to stdout.\nArgument: expected - String. Required. A string NOT to expect in the output\nArgument: binary - Callable. Required. Binary to run and evaluate output\nArgument: ... - Arguments. Optional. Any additional arguments to `binary`.\nArgument: --exit - Assert exit status of process to be this number\nArgument: --stderr - Also include standard error in output checking\nArgument: --help - Flag. Optional. Display this help.\nArgument: --handler handler - Function. Optional. Use this error handler instead of the default error handler.\nArgument: --display - String. Optional. Display name for the condition.\nArgument: --debug - Flag. Optional. Debugging enabled for the assertion function.\nArgument: --line lineNumber - Integer. Optional. Line number of calling function. Typically this is not required as it is computed from the calling function using `--line-depth`.\nArgument: --line-depth depth - Integer. Optional. The depth in the stack of function calls to find the line number of the calling function.\nArgument: --stdout-match - String. Optional. One or more strings which must match `stdout` output.\nArgument: --stdout-no-match - String. Optional. One or more strings which must match `stdout` output.\nArgument: --stderr-ok - Flag. Optional. Output to `stderr` will not cause the test to fail.\nArgument: --stderr-match - String. Optional. One or more strings which must match `stderr` output. Implies `--stderr-ok`\nArgument: --stderr-no-match - String. Optional. One or more strings which must match NOT `stderr` output. Implies `--stderr-ok`\nArgument: --dump - Flag. Optional. Output `stderr` and `stdout` after test regardless.\nArgument: --dump-binary - Flag. Optional. Output `stderr` and `stdout` after test regardless, displayed as binary.\nArgument: --plumber - Flag. Optional. Wrap the test call with the `plumber` call to detect local leaks.\nArgument: --leak globalName - Zero or more. String. Allow global leaks for these globals when `--plumber` is enabled.\nArgument: --skip-plumber - Flag. Optional. Skip plumber check for function calls. When specified with `--plumber` the last occurrence on the command line is effective.\nArgument: --head - Flag. Optional. When outputting `stderr` or `stdout`, output the head of the file.\nArgument: --tail - Flag. Optional. When outputting `stderr` or `stdout`, output the tail of the file. (Default)\nReturn Code: 0 - If the output contains at least one occurrence of the string\nReturn Code: 1 - If output does not contain string\nExample:     assertOutputDoesNotContain Success complex-thing.sh --dry-run\nReviewed: 2023-11-12\n\n'
return_code=$'0 - If the output contains at least one occurrence of the string\n1 - If output does not contain string\n'
reviewed=$'2023-11-12\n'
sourceFile="bin/build/tools/test.sh"
sourceHash="1643b40c1684bb3bbf723c7097d1aba261079515"
sourceLine="953"
summary="Run a command and expect the output to not contain"
summaryComputed="true"
usage="assertOutputDoesNotContain expected binary [ ... ] [ --exit ] [ --stderr ] [ --help ] [ --handler handler ] [ --display ] [ --debug ] [ --line lineNumber ] [ --line-depth depth ] [ --stdout-match ] [ --stdout-no-match ] [ --stderr-ok ] [ --stderr-match ] [ --stderr-no-match ] [ --dump ] [ --dump-binary ] [ --plumber ] [ --leak globalName ] [ --skip-plumber ] [ --head ] [ --tail ]"
documentationPath="documentation/source/tools/assert.md"
