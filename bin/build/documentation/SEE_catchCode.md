[catchCode]({rel}tools/sugar-core.md#catchcode) - Run `command`, handle failure with `handler` with `code` and `command` ([source](https://github.com/zesk/build/blob/main/bin/build/identical/_sugar.sh#L215))
