# shellcheck disable=SC2034
base="APPLICATION_JSON_PREFIX.sh"
category="Application"
derivations=([0]="env" [1]="envMarker")
description="Prefix to place we can store things in the JSON file (e.g. to set the fingerprint)"$'\n'""$'\n'""
descriptionLineCount="2"
env="APPLICATION_JSON_PREFIX"
envMarker="application_json_prefix"
file="bin/build/env/APPLICATION_JSON_PREFIX.sh"
fn="APPLICATION_JSON_PREFIX"
foundNames=([0]="type" [1]="category")
rawComment="Type: String"$'\n'"Category: Application"$'\n'"Prefix to place we can store things in the JSON file (e.g. to set the fingerprint)"$'\n'""$'\n'""
sourceFile="bin/build/env/APPLICATION_JSON_PREFIX.sh"
sourceHash="504bb5cb03f9f6c7ab63917eb5647cb5a8e1e432"
sourceLine=""
summary="Prefix to place we can store things in the JSON"
summaryComputed="true"
type="String"
