# shellcheck disable=SC2034
base="DEPLOY_REMOTE_HOME.sh"
category="Deployment"
derivations=([0]="env" [1]="envMarker")
description=$'Path on the remote server where the application deployment home is (per application)\n\n'
descriptionLineCount="2"
env="DEPLOY_REMOTE_HOME"
envMarker="deploy_remote_home"
file="bin/build/env/DEPLOY_REMOTE_HOME.sh"
fn="DEPLOY_REMOTE_HOME"
foundNames=([0]="category" [1]="type")
rawComment=$'Path on the remote server where the application deployment home is (per application)\nCategory: Deployment\nType: RemoteDirectory\n\n'
sourceFile="bin/build/env/DEPLOY_REMOTE_HOME.sh"
sourceHash="ca6689833b0b62c969a129efa25642d77fa83af9"
sourceLine=""
summary="Path on the remote server where the application deployment home"
summaryComputed="true"
type="RemoteDirectory"
