# shellcheck disable=SC2034
base="BUILD_DOCKER_IMAGE.sh"
category="Docker"
derivations=([0]="env" [1]="envMarker")
description=$'Default docker image to use when launching `dockerLocalContainer`\n\n'
descriptionLineCount="2"
env="BUILD_DOCKER_IMAGE"
envMarker="build_docker_image"
file="bin/build/env/BUILD_DOCKER_IMAGE.sh"
fn="BUILD_DOCKER_IMAGE"
foundNames=([0]="see" [1]="category" [2]="type")
rawComment=$'Default docker image to use when launching `dockerLocalContainer`\nSee: dockerLocalContainer\nCategory: Docker\nType: String\n\n'
see=$'dockerLocalContainer\n'
sourceFile="bin/build/env/BUILD_DOCKER_IMAGE.sh"
sourceHash="c8846eacb189e184a607285753a365be05939058"
sourceLine=""
summary="Default docker image to use when launching \`dockerLocalContainer\`"
summaryComputed="true"
type="String"
