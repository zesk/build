### `returnArgument`

> Return `argument` error code. Outputs `message ...` to `stderr`.

#### Usage

    returnArgument [ message ... ]

Return `argument` error code. Outputs `message ...` to `stderr`.



> Location: `bin/build/tools/_sugar.sh`

#### Arguments

- `message ...` - String. Optional. Message to output.

#### Reads standard input

{stdin}

#### Writes to standard output

{stdout}

#### Writes to standard error

{stderr}

#### Debugging settings

Append to the value of `BUILD_DEBUG` (a comma-delimited (`,`) list) and add these tokens to enable debugging:

{build_debug}

#### Examples

{example}

#### Sample Output

{output}

#### Return codes

- 2

#### Local cache

{local_cache}

#### Environment

{environment}

#### Requires



#### See Also

{see}

#### Credits

Thanks to [{credits}]({source}).

#### Review Status

File `bin/build/tools/_sugar.sh`, function `returnArgument` was reviewed {reviewed}.

#### Errors

{error}
