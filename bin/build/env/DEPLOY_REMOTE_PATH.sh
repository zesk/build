#!/usr/bin/env bash
# Path on the remote server where the application deployment home is (per application)
# Copyright &copy; 2025 Market Acumen, Inc.
# Category: Deployment
export DEPLOY_REMOTE_PATH
DEPLOY_REMOTE_PATH="${DEPLOY_REMOTE_PATH-}"
